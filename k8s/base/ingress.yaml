apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: go-todo
spec:
  ingressClassName: nginx
  rules:
    - host: todo.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: go-todo
                port:
                  number: 80